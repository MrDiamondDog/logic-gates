{
  "name": "logic-sim",
  "version": "1.0.0",
  "description": "Cool logic gate simulator",
  "main": "index.html",
  "scripts": {
    "test": "npm i --include=dev && concurrently --kill-others \"tsc -w\" \"node test.ex.js\"",
    "test-no-live": "npm i --include=dev && tsc -w",
    "build": "npm i && tsc && babel js -d js"
  },
  "keywords": ["Logic", "Logic Gates", "JavaScript", "Typescript"],
  "author": "MrDiamond",
  "license": "ISC",
  "dependencies": {
    "ascii-table": "^0.0.9",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.20.7",
    "@babel/core": "^7.20.12",
    "concurrently": "^7.6.0",
    "express": "^4.18.2"
  }
}
