{
  "name": "logic-sim",
  "version": "1.0.0",
  "description": "Cool logic gate simulator",
  "main": "index.html",
  "scripts": {
    "test": "npm i --include=dev && concurrently --kill-others \"tsc -w\" \"node test.ex.js\"",
    "test-no-live": "npm i --include=dev && concurrently --kill-others \"tsc -w\"",
    "build": "npm i && tsc && babel js --out-dir js --no-comments --compact true"
  },
  "keywords": ["Logic", "Logic Gates", "JavaScript", "Typescript"],
  "author": "MrDiamond",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.9.4",
    "@babel/cli": "^7.20.7",
    "@babel/core": "^7.20.12"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "express": "^4.18.2"
  }
}
